<VirtualHost *:80>
	ServerName cgserver.tsing.net

	Proxypass /.well-known/acme-challenge !
	Proxypass /phpmyadmin/ http://phpmyadmin/
	Proxypass / http://cgserver/
	ProxyPreserveHost On
	Alias /.well-known/acme-challenge /srv/ftp/cgserver/.well-known/acme-challenge
	<Directory /srv/ftp/cgserver/.well-known/acme-challenge/>
		require all granted
	</Directory>

	ErrorLog ${APACHE_LOG_DIR}/error.log
	CustomLog ${APACHE_LOG_DIR}/access.log combined

	#RewriteEngine on
	#RewriteCond %{SERVER_NAME} =cgserver.tsing.net
	#RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R]
</VirtualHost>
